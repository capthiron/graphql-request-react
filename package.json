{
  "name": "graphql-request-react",
  "version": "0.0.5",
  "description": "React wrapper for graphql-request",
  "main": "index.js",
  "repository": "https://github.com/Capthiron/graphql-request-react",
  "author": "Dario Leittersdorf <dario97@gmx.de>",
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.8.6"
  },
  "scripts": {
    "test": "echo \"Running tests for graphql-request-react\"",
    "preversion": "yarn test",
    "postversion": "git checkout -b release/$npm_package_version && git push --tags && yarn publish . --tag $npm_package_version && git push && echo \"Successfully released version $npm_package_version!\" && git checkout -b master"
  }
}
